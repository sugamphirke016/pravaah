<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background"
        android:drawable="@drawable/rounded_progress_bar_background"/>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="20dp" /> <!-- Adjust the radius to match the corners of the background -->
                <solid android:color="@color/ripple_todo_logo" /> <!-- Adjust the color as needed -->
            </shape>
        </clip>
    </item>
</layer-list>
